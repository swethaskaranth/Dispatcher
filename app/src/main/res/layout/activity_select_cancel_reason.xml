<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".features.lastmile.tripCreation.ui.activity.SelectCancelReason">

    <include
        android:id="@+id/layout_cancel_reason"
        layout="@layout/layout_cancel_reason"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/saveLayout"/>

    <include
        android:id="@+id/saveLayout"
        layout="@layout/layout_save_button"
        />

</androidx.constraintlayout.widget.ConstraintLayout>